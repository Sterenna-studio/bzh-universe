# Lemegeton sprite preview

Archive web locale du preview fourni avec le pack Lemegeton.

Le HTML source referencait aussi `deplacement_left.gif`, `deplacement_right.gif`, `merged_left.gif` et `merged_right.gif`, mais ces fichiers n'etaient pas presents dans le lot importe. La page `index.html` a donc ete corrigee pour ne pointer que vers les fichiers disponibles.
